#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define rep(i, j, k) for(int i=j; i<=k; i++) 
#define pii pair<int, int>
#define f first
#define s second

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	return 0;
}
